---
type: Atomik Project Log
title: atomik-dsl — log
---

# Log

## 2026-07-06

- Corpus stress test, 4 batches (30 ruptures across structural/temporal/epistemic expressiveness, small-model generability, pedagogical gestures). North-star fixed: the misconception scene (predict-then-see). → `sources/`
- Language spec **v0.3** shipped: 16 keywords / 4 planes, closed status vocabulary, addressable relations, authored/generated profiles. Pocket spec holds the ≤2K-token founding constraint (~1.1K). Accessible guide (fr).
- Render-core spec **v0.1** shipped: Scene IR (decisions D1–D11), layout contracts for 11 archetypes (L1–L5), pure runtime, theme-token obligations. Golden north-star fixture created (canonical source + expected IR + runtime oracle).
- Four errata fed back to the language (C1 reveal-anywhere hides initially; C2 `set` forbidden in rules; C3 transient notes; C4 gated-step semantics — found while writing the fixture's oracle).
- `cycle` archetype **end-to-end**: parser → IR → ring layout with satellites → pure runtime → interactive SVG prototype. **41/41 tests**, including byte-exact fixture parity and oracle A1–A7. Guide updated with the rendering section; C1–C4 now author-facing.

## 2026-07-07

- Decision: standalone dual-plane library repository, same template as the main Atomik repo; vendoring target `packages/dsl-core` at main-repo M12 (**ADR-DSL-001**).
- Repository initialized from the template. Register created (D1–D4). **CP-DSL-001** (v0.3.1 consolidation + package hardening) opened, active. **CP-DSL-004** (workbench integration) drafted early at owner request; executes in the main repo; scope freezes at its S00.
- Test suite verified from the package layout: 41 passed / 0 failed.
- **CP-DSL-001 S02** — language spec consolidated to **v0.3.1**: C1 (reveal-anywhere initial hiding, reveal+hide → hidden), C2 (`set` illegal in rules both profiles; the split effect grammar also records kernel-enforced step-only `require`), C3 (transient notes), C4 (gated steps withhold their own effects + block advancing; commitment-by-default spelled out) folded into §§2–6 + changelog §14. Pocket spec updated (effects line, visibility/gate note, gate comment in the choreography example): ≈1.2K tokens, ≤2K budget holds. Surface unchanged — files still begin `atomik 0.3`; golden fixture untouched; 41/41 green.
- **CP-DSL-001 S03** — render-core: **§6.2 per-archetype implementation notes** added with the `cycle` entry (longest-directed-cycle ring selection, earliest-IR-node rotation at 12 o'clock, radius formula `max(130, Σw/2π×1.55+44, N×44)`, 0.42-rad satellite fan, parked row + loud notice, ring-arc vs straight edges, L4 full-graph layout); contract table untouched. §10 marked **folded at v0.3.1** and now lists **C4** (was only in §5); intro corrected from "three errata (C1–C3)" to four; inline "candidate" marks retitled. 41/41 green.
- **CP-DSL-001 S04** — kernel split (verbatim moves, no behavior change): `src/lang.js` (text → IR; exports `parse`, `constants`) / `src/render.js` (pure runtime + layouts) / `src/index.js` (assembles the unchanged public surface; package `main`); `atomik_core.js` deleted. Tests now require the package entry; `build.mjs` concatenates the three files at `@CORE@` (browser globals `AtomikLang` → `AtomikRender` → `Atomik`); prototype rebuilt. Verified beyond the suite: 28-comparison A/B equivalence old-vs-new (IR/present/export/layout byte-identical as JSON), VM smoke of the browser UMD branch (fixture deep-equal, C4 gate, cycle ring). 41/41 + build green. Module note records the internal layout as non-contractual.
- **CP-DSL-001 S05** — `src/index.d.ts` added and wired via the package `types` field: `SceneIR` mirrors render-core §2.2 verbatim (frozen contract; `scene`/`claim` nullable for partial validity), runtime `Presentation` + layout `Geometry` typed from the current emit and marked engine-internal, UMD global + `require` both typed (`export as namespace`). Verified with `tsc --strict` on a consumer script: narrowing across `Ref`/`EffectIR`/`LayoutEdge` unions compiles, four `@ts-expect-error` negatives hold, exit 0. Guide (fr) cross-checked against v0.3.1: C1–C4 teaching confirmed (Niveau 5, piège 6); fixed stale pointers — prototype path (`apps/prototype-cycle/index.html`), kernel layout (`src/{lang,render,index}.js`), pocket-spec size (≈1,2K), spec §14 pointer, antisèche now marks `require` *and* `set` step-only. 41/41 + build green.
- **CP-DSL-001 S06 — path closed.** Definition-of-done audited item-by-item (record in the ledger): specs consolidated and cross-marked (language §14 ↔ render-core §10), §6.2 in place, split kernel with identical public surface + types, guide verified, **golden fixture byte-identical to the base commit across the whole path** (`git diff ba27ef9..HEAD -- packages/dsl-core/fixtures/` empty), 41/41 green at every step, docs updated in the same work unit throughout, no scope widening. Register: **D1 → done**. Orientation roadmap updated. `ACTIVE.md` → none; **D2** (`flow`) is next, opens just-in-time per the register rule.
- **CP-DSL-002 S01 — path opened** (owner go-ahead in session; template read from main-repo bedrock 24 at `~/projects/4tom1k`). Scope: the `flow` archetype as a real layered engine against §6 (ranks follow edges, lanes, routed back-edges, L1–L5), the contractual `cycle`→`flow` no-cycle fallback, §6.2 flow entry, prototype flip. Render-side only — the IR must not move. Base commit 5064ff7.
- **CP-DSL-002 S02** — `layoutFlow` core landed in `src/render.js` (Sugiyama-lite, wholly deterministic from IR order): DFS cycle-breaking → back-edge set, longest-path ranking of the acyclic remainder, two mean-field ordering passes with stable IR-order ties, centered rows sized from `nodeBox` (L2 by construction), straight edges, back-edges flagged for S03 routing. `layout()` dispatches `flow`; `layoutFlow` exported. **Suite 6: +13 assertions → 54/54 green** (rank-order §6 property on a diamond DAG and on the demo-as-flow, L1 byte-identical re-layout, L2 no-overlap, L4 hidden-nodes-positioned, single back-edge closing the water cycle). Test-count references refreshed (README, AGENTS, module note, guide, index). Fixture untouched.
- **CP-DSL-002 S03** — `flow` meets the last two cells of its §6 row: **lanes** (declaration-ordered x-bands from `group [kind lane]`, per-band centering, members provably inside their band, regions exposed as `layout.lanes` for painting) and **routed back-edges** (6-point orthogonal path through the node-free inter-row corridors and up the right flank, staggered per edge; geometry keeps the straight coords next to `path` so the current painter stays correct until S05 teaches it paths). Suite 7: +8 assertions → **62/62 green**, including a segment-vs-box sweep proving the route clears every node. Fixture untouched.
- **CP-DSL-002 S04** — contractual fallback wired: `project as cycle` on a non-cyclic model now re-enters the `flow` engine with a loud L5 notice (the minimal grid stays only for unimplemented archetypes). Render-core **§6.2 flow entry** written (cycle-breaking, longest-path ranking, mean-field ordering, lane bands, corridor routing, spacing constants, L4); the cycle entry's "until `flow` exists" caveat retired. Suite 8: +3 assertions → **65/65 green**. Fixture untouched.
- **CP-DSL-002 S05** — flow surfaced end-to-end: the painter prefers a pre-routed `path` on any edge (back-edges now draw around the diagram), lane regions paint from `layout.lanes` (new `.lane`/`.lanelabel` styles), boundary glyphs anchor on `labelAt`; prototype rebuilt. Guide's prototype-limits sentence names both real archetypes and the flip between them. `index.d.ts`: `Geometry` carries the flow fields (`rows`/`backEdges`/`lanes`; `ring`/`parked` now optional), `LayoutEdge` gains the back+path variant, `layoutFlow` declared, `ArchetypeAttempt` replaces `CycleAttempt`. Verified: `tsc --strict` exit 0 on the consumer script; browser-branch VM smoke (two lanes, routed back-edge, built html carries engine + styles + painter). 65/65 + build green.
- **CP-DSL-002 S06 — path closed.** Definition-of-done audited line-by-line (record in the ledger): the §6 flow row is met and property-tested (rank order, lanes, routed back-edges, cycles legal), L1–L5 hold, the `cycle`→`flow` contractual fallback replaced the grid, §6.2 documents the engine, and the path stayed render-side — `git diff 5064ff7..HEAD` is empty on the fixture, the language spec, and the pocket spec, and never touches `src/lang.js`. **65/65 green.** Register: **D2 → done**. `ACTIVE.md` → none. **Next: D3** (generability evaluation) — deliberately not opened: model access, spend, and corpus scope are owner decisions; **D4** additionally requires the S00 register amendment in the main repository.
