---
type: Atomik Note
title: Open questions (provisional)
tags: [brainstorm, provisional]
timestamp: 2026-07-07T00:00:00Z
---

# Open questions — provisional thinking, may be promoted

- Multi-group membership: a node in a `lane` *and* a `loop` (deferred in IR 0.1; single `group` field).
- A builtin `step` identifier in rule expressions (currently: no; revisit after real authoring).
- Soft cap guidance (~40 nodes/scene) vs per-archetype caps declared by contracts.
- `cycle` satellites: fan spread vs lane-orbits when one anchor accumulates many satellites.
- Unresolved-ref affordance in *exported* SVG (in-app it opens create-note; on paper it is just dotted).
- Canonical printer scope for v0.3.1 vs integration time (flip only needs a one-line rewrite).
- Naming: the app is Atomik, the DSL is atomik; fence tag is ` ```atomik `. Confirm nothing else needs the distinction.
